# Cron Job: kanban-dispatch

**Job ID:** 76ec3743767d
**Run Time:** 2026-05-09 20:17:01
**Schedule:** */2 * * * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

## Script Error
The data-collection script failed. Report this to the user.

```
Script exited with code 2
stderr:
usage: hermes [-h] [--version] [--resume SESSION] [--continue [SESSION_NAME]]
              [--worktree] [--skills SKILLS] [--yolo] [--pass-session-id]
              {chat,model,gateway,setup,whatsapp,login,logout,auth,status,cron,webhook,doctor,dump,debug,backup,import,config,pairing,skills,plugins,memory,tools,mcp,sessions,insights,claw,version,update,uninstall,acp,profile,completion,dashboard,logs}
              ...
hermes: error: argument command: invalid choice: 'kanban' (choose from 'chat', 'model', 'gateway', 'setup', 'whatsapp', 'login', 'logout', 'auth', 'status', 'cron', 'webhook', 'doctor', 'dump', 'debug', 'backup', 'import', 'config', 'pairing', 'skills', 'plugins', 'memory', 'tools', 'mcp', 'sessions', 'insights', 'claw', 'version', 'update', 'uninstall', 'acp', 'profile', 'completion', 'dashboard', 'logs')
```



## Response

## Script Error Report: `kanban-dispatch` Cron Job

### Error Summary
The `kanban-dispatch` cron job is failing because it invokes a **non-existent CLI subcommand**.

| | |
|---|---|
| **Job** | `kanban-dispatch` (`76ec3743767d`) |
| **Schedule** | `*/2 * * * *` (every 2 minutes) |
| **Script** | `~/.hermes/scripts/dispatch.sh` |
| **Error** | `hermes: error: argument command: invalid choice: 'kanban'` |

### Root Cause
Your installed Hermes version (`v0.10.0`, built 2026-04-16) is **89 commits behind** the current version. The `hermes kanban` subcommand family (`hermes kanban dispatch`, `hermes kanban init`, `hermes kanban create`, etc.) was added in a more recent release. Your `dispatch.sh` script was written for that newer version, so it fails on every run.

### Evidence
- `dispatch.sh` contents:
  ```bash
  #!/bin/bash
  hermes kanban dispatch
  ```
- `hermes --version` output: `v0.10.0 (2026.4.16)` — 89 commits behind
- `hermes --help` shows no `kanban` subcommand in the available choices
- Your own memory notes confirm this cron was set up as a workaround because the gateway's built-in kanban dispatcher doesn't load on older versions

### Fix
Update Hermes to the latest version:

```bash
hermes update
```

After updating, verify the command exists:

```bash
hermes kanban --help
```

Then the cron job will work as intended. If you want to avoid repeated failure notifications until you update, you can temporarily disable the job:

```bash
hermes cron disable 76ec3743767d
```

And re-enable it after updating:

```bash
hermes cron enable 76ec3743767d
```
