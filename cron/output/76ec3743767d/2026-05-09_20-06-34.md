# Cron Job: kanban-dispatch

**Job ID:** 76ec3743767d
**Run Time:** 2026-05-09 20:06:34
**Schedule:** */2 * * * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

## Script Error
The data-collection script failed. Report this to the user.

```
Script exited with code 1
stderr:
File "/Users/luoyang/.hermes/scripts/dispatch.sh", line 3
    hermes kanban dispatch
           ^^^^^^
SyntaxError: invalid syntax
```



## Response

The script itself is correct, but **the Hermes cron scheduler has a bug** that causes it to run *all* scripts with Python — even shell scripts.

**Root cause:** In `/Users/luoyang/hermes-agent/cron/scheduler.py` (line 457), the `_run_job_script` function unconditionally invokes every script via `sys.executable` (Python):

```python
result = subprocess.run(
    [sys.executable, str(path)],   # ← Always runs `python <script>`
    capture_output=True,
    ...
)
```

Your `dispatch.sh` has a valid bash shebang (`#!/bin/bash`) and is executable, but the scheduler ignores both and passes it to the Python interpreter. Python then tries to parse `hermes kanban dispatch` as Python code and fails with `SyntaxError`.

**The error is in the scheduler, not your script.**

### How to fix it

You have two options:

**Option 1 – Convert to a Python script (fastest workaround)**
Rename the script to `dispatch.py` and rewrite it to use `subprocess`:

```python
#!/usr/bin/env python3
import subprocess
subprocess.run(["hermes", "kanban", "dispatch"], check=True)
```

Then update the cron job:
```bash
hermes cron edit 76ec3743767d --script dispatch.py
```

**Option 2 – Patch the scheduler to support shell scripts**
Edit `/Users/luoyang/hermes-agent/cron/scheduler.py` and change the `subprocess.run` call to respect the file extension:

```python
# Around line 456
if str(path).endswith(".sh"):
    cmd = ["/bin/bash", str(path)]
else:
    cmd = [sys.executable, str(path)]

result = subprocess.run(
    cmd,
    capture_output=True,
    text=True,
    timeout=script_timeout,
    cwd=str(path.parent),
)
```

Then restart the Hermes gateway / daemon so the patched module is loaded.

---

**Summary:** The cron scheduler only supports Python scripts right now, despite accepting any file path. Your `.sh` dispatcher keeps failing because Python tries to execute it. Converting to a Python wrapper (Option 1) is the quickest fix.
