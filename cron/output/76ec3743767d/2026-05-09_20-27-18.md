# Cron Job: kanban-dispatch

**Job ID:** 76ec3743767d
**Run Time:** 2026-05-09 20:27:18
**Schedule:** */2 * * * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

## Script Error
The data-collection script failed. Report this to the user.

```
Script exited with code 2
stderr:
usage: hermes [-h] [--version] [--resume SESSION] [--continue [SESSION_NAME]]
              [--worktree] [--skills SKILLS] [--yolo] [--pass-session-id]
              {chat,model,gateway,setup,whatsapp,login,logout,auth,status,cron,webhook,doctor,dump,debug,backup,import,config,pairing,skills,plugins,memory,tools,mcp,sessions,insights,claw,version,update,uninstall,acp,profile,completion,dashboard,logs}
              ...
hermes: error: argument command: invalid choice: 'kanban' (choose from 'chat', 'model', 'gateway', 'setup', 'whatsapp', 'login', 'logout', 'auth', 'status', 'cron', 'webhook', 'doctor', 'dump', 'debug', 'backup', 'import', 'config', 'pairing', 'skills', 'plugins', 'memory', 'tools', 'mcp', 'sessions', 'insights', 'claw', 'version', 'update', 'uninstall', 'acp', 'profile', 'completion', 'dashboard', 'logs')
```



## Response

## Cron Job Failure Report

**Status:** ❌ Script failed with exit code 2

**Error Summary:**  
The data-collection script invoked `hermes kanban`, but **`kanban` is not a valid Hermes CLI subcommand** in your current installation.

**Valid commands** include: `chat`, `model`, `gateway`, `setup`, `cron`, `webhook`, `doctor`, `skills`, `tools`, `mcp`, `sessions`, `insights`, etc. — but **`kanban` is absent**.

### Likely Cause
The script was written assuming a `hermes kanban` CLI command exists. However, kanban functionality in Hermes is provided via **skills** (`kanban-orchestrator`, `kanban-worker`), not a top-level CLI subcommand.

### Suggested Fix
1. **Review the script** in `~/.hermes/scripts/` (or wherever the cron job's `--script` points).
2. **Replace** any occurrence of:
   ```bash
   hermes kanban ...
   ```
   with the appropriate valid command. Depending on intent, this might be:
   - `hermes chat` with kanban-specific prompts
   - `hermes skills` to invoke kanban skills
   - Another CLI command entirely
3. If the script was copied from documentation or another environment, verify it matches your installed Hermes version.

### Quick Diagnostic
Run the following to inspect the offending script:
```bash
hermes cron list
# Note the script name, then:
cat ~/.hermes/scripts/<script-name>
```

Look for the line containing `hermes kanban` and update it to use a valid subcommand.
