# Cron Job: kanban-dispatch

**Job ID:** 76ec3743767d
**Run Time:** 2026-05-10 09:36:30
**Schedule:** */2 * * * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

## Script Error
The data-collection script failed. Report this to the user.

```
Script exited with code 2
stderr:
usage: hermes [-h] [--version] [--resume SESSION] [--continue [SESSION_NAME]]
              [--worktree] [--skills SKILLS] [--yolo] [--pass-session-id]
              {chat,model,gateway,setup,whatsapp,login,logout,auth,status,cron,webhook,doctor,dump,debug,backup,import,config,pairing,skills,plugins,memory,tools,mcp,sessions,insights,claw,version,update,uninstall,acp,profile,completion,dashboard,logs}
              ...
hermes: error: argument command: invalid choice: 'kanban' (choose from 'chat', 'model', 'gateway', 'setup', 'whatsapp', 'login', 'logout', 'auth', 'status', 'cron', 'webhook', 'doctor', 'dump', 'debug', 'backup', 'import', 'config', 'pairing', 'skills', 'plugins', 'memory', 'tools', 'mcp', 'sessions', 'insights', 'claw', 'version', 'update', 'uninstall', 'acp', 'profile', 'completion', 'dashboard', 'logs')
```



## Response

## Script Failure Report: `hermes kanban` — Invalid Command

**Error:** The data-collection script invoked `hermes kanban ...`, but **`kanban` is not a valid subcommand** in the currently installed version of the Hermes CLI.

**Exit code:** 2

**Valid Hermes commands on this system:**
`chat`, `model`, `gateway`, `setup`, `whatsapp`, `login`, `logout`, `auth`, `status`, `cron`, `webhook`, `doctor`, `dump`, `debug`, `backup`, `import`, `config`, `pairing`, `skills`, `plugins`, `memory`, `tools`, `mcp`, `sessions`, `insights`, `claw`, `version`, `update`, `uninstall`, `acp`, `profile`, `completion`, `dashboard`, `logs`

**Likely causes:**
1. **Version mismatch** — The `kanban` CLI commands may have been removed, renamed, or not yet merged into the installed build.
2. **Plugin not loaded** — Kanban functionality might require a plugin (e.g., `hermes plugins enable ...`) that isn't active.
3. **Wrong interface** — The Kanban skills/docs reference `hermes kanban <verb>` for human operators, but if the current CLI build doesn't expose it, those calls will fail.

**Recommended fix:**
- Check `hermes --version` and compare with the version the script was written for.
- If kanban is now a dashboard/web feature, try `hermes dashboard` or `hermes sessions` instead.
- If the script only needs to query or manage tasks programmatically, you may need to use the HTTP API or a different CLI command (e.g., `hermes cron` if it's a scheduled-job system, or `hermes sessions` if it's session tracking).

Please update the data-collection script to use a command that exists in the current Hermes CLI, or upgrade/reconfigure Hermes to restore the `kanban` subcommand.
