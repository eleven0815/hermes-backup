# Cron Job: kanban-dispatch

**Job ID:** 76ec3743767d
**Run Time:** 2026-05-09 20:38:32
**Schedule:** */2 * * * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

## Script Error
The data-collection script failed. Report this to the user.

```
Script exited with code 2
stderr:
usage: hermes [-h] [--version] [--resume SESSION] [--continue [SESSION_NAME]]
              [--worktree] [--skills SKILLS] [--yolo] [--pass-session-id]
              {chat,model,gateway,setup,whatsapp,login,logout,auth,status,cron,webhook,doctor,dump,debug,backup,import,config,pairing,skills,plugins,memory,tools,mcp,sessions,insights,claw,version,update,uninstall,acp,profile,completion,dashboard,logs}
              ...
hermes: error: argument command: invalid choice: 'kanban' (choose from 'chat', 'model', 'gateway', 'setup', 'whatsapp', 'login', 'logout', 'auth', 'status', 'cron', 'webhook', 'doctor', 'dump', 'debug', 'backup', 'import', 'config', 'pairing', 'skills', 'plugins', 'memory', 'tools', 'mcp', 'sessions', 'insights', 'claw', 'version', 'update', 'uninstall', 'acp', 'profile', 'completion', 'dashboard', 'logs')
```



## Response

## Cron Job Failure Report

**Job:** `kanban-dispatch` (ID: `76ec3743767d`)  
**Schedule:** Every 2 minutes (`*/2 * * * *`)  
**Script:** `~/.hermes/scripts/dispatch.sh`

### What Failed
The script exits with code 2 because it calls a non-existent CLI command:

```bash
#!/bin/bash
# Kanban Auto-Dispatcher
hermes kanban dispatch
```

### Root Cause
`hermes kanban` is **not a valid subcommand** in Hermes v0.10.0. The available commands are:
`chat`, `model`, `gateway`, `setup`, `whatsapp`, `login`, `logout`, `auth`, `status`, `cron`, `webhook`, `doctor`, `dump`, `debug`, `backup`, `import`, `config`, `pairing`, `skills`, `plugins`, `memory`, `tools`, `mcp`, `sessions`, `insights`, `claw`, `version`, `update`, `uninstall`, `acp`, `profile`, `completion`, `dashboard`, `logs`

There is no `kanban` top-level command.

### Context
You do have `kanban-orchestrator` and `kanban-worker` **skills** installed, but these are in-session Python tools (e.g. `kanban_create()`, `kanban_dispatch()`) used by Hermes Agent during conversations — they are **not** CLI subcommands invocable from a shell script.

### Fix Needed
Edit `~/.hermes/scripts/dispatch.sh` to use a valid command. The current script cannot work as written. If you want to trigger kanban dispatching via cron, you likely need to run it through a Hermes chat session or gateway instead, e.g.:

```bash
hermes chat --prompt "kanban_dispatch()"
```

Or, if there is a different intended command, replace `hermes kanban dispatch` with the correct invocation.

### Recommended Action
1. Pause or delete the broken job:
   ```bash
   hermes cron pause kanban-dispatch
   ```
2. Fix `~/.hermes/scripts/dispatch.sh` with the correct command.
3. Re-enable once verified.

Until fixed, this job will continue failing every 2 minutes.
