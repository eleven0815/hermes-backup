# Cron Job: kanban-dispatch

**Job ID:** 76ec3743767d
**Run Time:** 2026-05-09 20:18:18
**Schedule:** */2 * * * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

## Script Error
The data-collection script failed. Report this to the user.

```
Script exited with code 2
stderr:
usage: hermes [-h] [--version] [--resume SESSION] [--continue [SESSION_NAME]]
              [--worktree] [--skills SKILLS] [--yolo] [--pass-session-id]
              {chat,model,gateway,setup,whatsapp,login,logout,auth,status,cron,webhook,doctor,dump,debug,backup,import,config,pairing,skills,plugins,memory,tools,mcp,sessions,insights,claw,version,update,uninstall,acp,profile,completion,dashboard,logs}
              ...
hermes: error: argument command: invalid choice: 'kanban' (choose from 'chat', 'model', 'gateway', 'setup', 'whatsapp', 'login', 'logout', 'auth', 'status', 'cron', 'webhook', 'doctor', 'dump', 'debug', 'backup', 'import', 'config', 'pairing', 'skills', 'plugins', 'memory', 'tools', 'mcp', 'sessions', 'insights', 'claw', 'version', 'update', 'uninstall', 'acp', 'profile', 'completion', 'dashboard', 'logs')
```



## Response

## Cron Job Failure Report

**Job Status:** FAILED (Exit Code 2)

**Error Summary:**  
The data-collection script attempted to run `hermes kanban`, but `kanban` is **not a valid subcommand** in the installed version of the Hermes CLI.

**Available Commands:**  
`chat`, `model`, `gateway`, `setup`, `whatsapp`, `login`, `logout`, `auth`, `status`, `cron`, `webhook`, `doctor`, `dump`, `debug`, `backup`, `import`, `config`, `pairing`, `skills`, `plugins`, `memory`, `tools`, `mcp`, `sessions`, `insights`, `claw`, `version`, `update`, `uninstall`, `acp`, `profile`, `completion`, `dashboard`, `logs`

**Likely Causes & Fixes:**

1. **Outdated/Deprecated Command** — The `kanban` command may have been removed or renamed in a recent Hermes update. Check if it was replaced by `sessions`, `insights`, or a skill-based workflow.
2. **Plugin Not Loaded** — If `kanban` was provided by a plugin, verify the plugin is installed and enabled (`hermes plugins list`).
3. **Typo in Script** — Double-check the cron script for a typo. The intended command might be something else (e.g., `hermes cron`, `hermes sessions`, or a custom skill invocation).
4. **Version Mismatch** — The script may have been written for a different version of Hermes. Run `hermes --version` and compare with the expected CLI version.

**Recommended Next Steps:**  
- Review the cron job script to identify the exact `hermes kanban ...` invocation.  
- Check `hermes skills list` to see if kanban functionality is now accessed via a skill instead of a top-level command.  
- If `kanban` was recently removed, update the script to use the replacement command or reinstall the relevant plugin/extension.

Please share the cron script contents if you'd like help identifying the correct replacement command.
