# Cron Job: kanban-dispatch

**Job ID:** 76ec3743767d
**Run Time:** 2026-05-09 23:40:20
**Schedule:** */2 * * * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

## Script Error
The data-collection script failed. Report this to the user.

```
Script exited with code 2
stderr:
usage: hermes [-h] [--version] [--resume SESSION] [--continue [SESSION_NAME]]
              [--worktree] [--skills SKILLS] [--yolo] [--pass-session-id]
              {chat,model,gateway,setup,whatsapp,login,logout,auth,status,cron,webhook,doctor,dump,debug,backup,import,config,pairing,skills,plugins,memory,tools,mcp,sessions,insights,claw,version,update,uninstall,acp,profile,completion,dashboard,logs}
              ...
hermes: error: argument command: invalid choice: 'kanban' (choose from 'chat', 'model', 'gateway', 'setup', 'whatsapp', 'login', 'logout', 'auth', 'status', 'cron', 'webhook', 'doctor', 'dump', 'debug', 'backup', 'import', 'config', 'pairing', 'skills', 'plugins', 'memory', 'tools', 'mcp', 'sessions', 'insights', 'claw', 'version', 'update', 'uninstall', 'acp', 'profile', 'completion', 'dashboard', 'logs')
```



## Response

## Script Failure Report

**Status:** FAILED  
**Exit Code:** 2  
**Time:** 2026-05-09 23:39

### Error Summary
The data-collection script invoked an invalid Hermes CLI command:

```
hermes kanban
```

The `kanban` subcommand is **not recognized** by the installed `hermes` CLI. The valid commands are: `chat`, `model`, `gateway`, `setup`, `whatsapp`, `login`, `logout`, `auth`, `status`, `cron`, `webhook`, `doctor`, `dump`, `debug`, `backup`, `import`, `config`, `pairing`, `skills`, `plugins`, `memory`, `tools`, `mcp`, `sessions`, `insights`, `claw`, `version`, `update`, `uninstall`, `acp`, `profile`, `completion`, `dashboard`, `logs`.

### Likely Causes
1. **Missing plugin** — The `kanban` functionality may require a plugin that is not installed or enabled.
2. **Renamed/removed command** — The command may have been renamed or deprecated in the current version.
3. **Typo in script** — The script may contain an incorrect command name.

### Recommended Actions
- Check if a `kanban` plugin needs to be installed: `hermes plugins install <plugin-name>`
- Verify the intended command by running `hermes --help` or `hermes skills` to see available skills.
- Review the data-collection script for the exact line invoking `hermes kanban` and update it to the correct command.

If you need help identifying the correct replacement command or installing the required plugin, please share the script contents or the intended workflow.
