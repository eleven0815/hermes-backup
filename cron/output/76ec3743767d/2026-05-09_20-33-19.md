# Cron Job: kanban-dispatch

**Job ID:** 76ec3743767d
**Run Time:** 2026-05-09 20:33:19
**Schedule:** */2 * * * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

## Script Error
The data-collection script failed. Report this to the user.

```
Script exited with code 2
stderr:
usage: hermes [-h] [--version] [--resume SESSION] [--continue [SESSION_NAME]]
              [--worktree] [--skills SKILLS] [--yolo] [--pass-session-id]
              {chat,model,gateway,setup,whatsapp,login,logout,auth,status,cron,webhook,doctor,dump,debug,backup,import,config,pairing,skills,plugins,memory,tools,mcp,sessions,insights,claw,version,update,uninstall,acp,profile,completion,dashboard,logs}
              ...
hermes: error: argument command: invalid choice: 'kanban' (choose from 'chat', 'model', 'gateway', 'setup', 'whatsapp', 'login', 'logout', 'auth', 'status', 'cron', 'webhook', 'doctor', 'dump', 'debug', 'backup', 'import', 'config', 'pairing', 'skills', 'plugins', 'memory', 'tools', 'mcp', 'sessions', 'insights', 'claw', 'version', 'update', 'uninstall', 'acp', 'profile', 'completion', 'dashboard', 'logs')
```



## Response

## Cron Job Failure Report

**Status:** Script error — command not found  
**Exit code:** 2

### What happened

The data-collection script invoked `hermes kanban ...`, but **`kanban` is not a valid Hermes subcommand** in the currently installed version.

The CLI only accepts these top-level commands:
```
chat, model, gateway, setup, whatsapp, login, logout, auth, status,
cron, webhook, doctor, dump, debug, backup, import, config, pairing,
skills, plugins, memory, tools, mcp, sessions, insights, claw, version,
update, uninstall, acp, profile, completion, dashboard, logs
```

`kanban` is absent from that list, so the argument parser rejected it immediately.

### Likely causes

1. **Version mismatch** — The script was written for a newer (or different build) of Hermes that includes the Kanban feature, but the runtime environment is running an older version.
2. **Feature not installed** — Kanban may be a plugin or optional component that hasn't been enabled in this installation.
3. **Wrong command syntax** — The script may be trying to use an internal/API pattern via the CLI.

### How to fix

**Option A: Update Hermes**
If Kanban is expected to be available, upgrade to the version that includes it:
```bash
hermes update
```

**Option B: Check if Kanban is available via plugins**
```bash
hermes plugins list
# If a kanban plugin exists:
hermes plugins enable <kanban-plugin-name>
```

**Option C: Rewrite the script to avoid `hermes kanban` CLI calls**
The Kanban skills indicate that `kanban_*` tools are meant to be used inside agent runs, while the CLI is for human operators. If the script is running inside an agent context, it should use the native tools (e.g., `kanban_show`, `kanban_create`, `kanban_complete`) instead of shelling out to `hermes kanban <verb>`.

**Option D: Audit the cron script**
Check what the script is actually trying to do and where it lives:
```bash
hermes cron list
cat ~/.hermes/scripts/<script-name>
```

### Immediate next step

Please verify which version of Hermes is installed and whether Kanban is expected to be active:

```bash
hermes --version
hermes plugins list | grep -i kanban
```

If Kanban *should* be available but isn't, the installation may need an update or configuration change. If it *shouldn't* be available, the data-collection script needs to be rewritten to use supported commands only.
