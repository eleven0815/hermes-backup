# Cron Job: kanban-dispatch

**Job ID:** 76ec3743767d
**Run Time:** 2026-05-09 23:59:47
**Schedule:** */2 * * * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

## Script Error
The data-collection script failed. Report this to the user.

```
Script exited with code 2
stderr:
usage: hermes [-h] [--version] [--resume SESSION] [--continue [SESSION_NAME]]
              [--worktree] [--skills SKILLS] [--yolo] [--pass-session-id]
              {chat,model,gateway,setup,whatsapp,login,logout,auth,status,cron,webhook,doctor,dump,debug,backup,import,config,pairing,skills,plugins,memory,tools,mcp,sessions,insights,claw,version,update,uninstall,acp,profile,completion,dashboard,logs}
              ...
hermes: error: argument command: invalid choice: 'kanban' (choose from 'chat', 'model', 'gateway', 'setup', 'whatsapp', 'login', 'logout', 'auth', 'status', 'cron', 'webhook', 'doctor', 'dump', 'debug', 'backup', 'import', 'config', 'pairing', 'skills', 'plugins', 'memory', 'tools', 'mcp', 'sessions', 'insights', 'claw', 'version', 'update', 'uninstall', 'acp', 'profile', 'completion', 'dashboard', 'logs')
```



## Response

**Script Error Report**

The data-collection script failed because it attempted to invoke the Hermes CLI subcommand `kanban`, which does not exist in the currently installed version.

**Error details**
- Exit code: `2`
- Command used: `hermes kanban ...`
- Error: `invalid choice: 'kanban'`

**Available Hermes CLI commands on this system**
`chat`, `model`, `gateway`, `setup`, `whatsapp`, `login`, `logout`, `auth`, `status`, `cron`, `webhook`, `doctor`, `dump`, `debug`, `backup`, `import`, `config`, `pairing`, `skills`, `plugins`, `memory`, `tools`, `mcp`, `sessions`, `insights`, `claw`, `version`, `update`, `uninstall`, `acp`, `profile`, `completion`, `dashboard`, `logs`

**Likely causes**
1. **Missing plugin / feature not enabled.** The Kanban feature may require a plugin (e.g. `hermes plugins install kanban` or a separate package) that isn't present.
2. **CLI version mismatch.** The script may have been written for a newer or different build of Hermes where `kanban` is a top-level subcommand, but the installed version is older or was compiled without it.
3. **Command renamed.** The functionality may now live under a different namespace (e.g. `hermes tools kanban ...` or via MCP/native tools rather than CLI).

**Recommended next steps**
- Run `hermes version` and compare it against the version the script expects.
- Check `hermes plugins list` to see if a Kanban plugin is available but not installed.
- If Kanban is supposed to be built-in, try `hermes update` to refresh the CLI.
- If the script was recently written, verify whether it should be using the native `kanban_*` tools (e.g. `kanban_create`) inside an agent session rather than shelling out to `hermes kanban`.
