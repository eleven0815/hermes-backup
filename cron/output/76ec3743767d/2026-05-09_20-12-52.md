# Cron Job: kanban-dispatch

**Job ID:** 76ec3743767d
**Run Time:** 2026-05-09 20:12:52
**Schedule:** */2 * * * *

## Prompt

[SYSTEM: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

## Script Error
The data-collection script failed. Report this to the user.

```
Script exited with code 2
stderr:
usage: hermes [-h] [--version] [--resume SESSION] [--continue [SESSION_NAME]]
              [--worktree] [--skills SKILLS] [--yolo] [--pass-session-id]
              {chat,model,gateway,setup,whatsapp,login,logout,auth,status,cron,webhook,doctor,dump,debug,backup,import,config,pairing,skills,plugins,memory,tools,mcp,sessions,insights,claw,version,update,uninstall,acp,profile,completion,dashboard,logs}
              ...
hermes: error: argument command: invalid choice: 'kanban' (choose from 'chat', 'model', 'gateway', 'setup', 'whatsapp', 'login', 'logout', 'auth', 'status', 'cron', 'webhook', 'doctor', 'dump', 'debug', 'backup', 'import', 'config', 'pairing', 'skills', 'plugins', 'memory', 'tools', 'mcp', 'sessions', 'insights', 'claw', 'version', 'update', 'uninstall', 'acp', 'profile', 'completion', 'dashboard', 'logs')
```



## Response

## Script Error Report

**Status:** Failure — invalid CLI command  
**Exit code:** 2

### What happened
A data-collection script invoked the Hermes CLI with the subcommand `kanban`:

```bash
hermes kanban ...
```

The CLI rejected it because **`kanban` is not a valid subcommand** in the currently installed version.

### Installed version
- **Hermes Agent v0.10.0** (2026-04-16)
- 89 commits behind the latest release

### Valid subcommands in this version
`chat`, `model`, `gateway`, `setup`, `whatsapp`, `login`, `logout`, `auth`, `status`, `cron`, `webhook`, `doctor`, `dump`, `debug`, `backup`, `import`, `config`, `pairing`, `skills`, `plugins`, `memory`, `tools`, `mcp`, `sessions`, `insights`, `claw`, `version`, `update`, `uninstall`, `acp`, `profile`, `completion`, `dashboard`, `logs`

No `kanban` command exists in the codebase or skills directory.

### Likely causes
1. **Version mismatch:** The script was written for a newer version of Hermes that includes a `kanban` command.
2. **Typo:** The script may have meant `hermes cron` or another existing subcommand.
3. **Missing skill/plugin:** The `kanban` functionality might be intended as a skill invocation rather than a top-level CLI command.

### Recommended fixes
1. **Check the script source** for the exact `hermes kanban` invocation and confirm whether it should be a different command (e.g., `hermes cron ...`).
2. **Update Hermes** if `kanban` was added in a newer release:
   ```bash
   hermes update
   ```
3. **If `kanban` is a skill**, invoke it via the appropriate mechanism rather than as a CLI subcommand.

If you share the script contents or the cron job definition, I can pinpoint the exact fix.
